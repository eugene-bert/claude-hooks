Enable Telegram notifications for the current Claude Code session. Run this bash command exactly:

```bash
touch ~/.claude/hooks/.notify-arm && echo "Notifications armed — will activate on next action"
```

Report the output to the user.
